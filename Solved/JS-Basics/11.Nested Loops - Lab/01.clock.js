function clock() {
    for (let hrs = 0; hrs < 24; hrs++) {
        for (let min = 0; min < 60; min++) {
            for (let sec = 0; sec < 60; sec++) {

                console.log(`${hrs}:${min}:${sec}`);

            }
        }
    }




}
clock()